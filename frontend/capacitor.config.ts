import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.suyashnimse.budgetmonitoring',
  appName: 'budget-monitoring-system',
  webDir: 'dist/frontend/browser'
};

export default config;
